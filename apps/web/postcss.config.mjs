/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind CSS v4 — the plugin bundles autoprefixer/import handling.
    "@tailwindcss/postcss": {},
  },
};

export default config;
