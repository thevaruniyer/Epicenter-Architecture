import { StudentPlaceholder } from "@/components/student/placeholder";
export default function StudentRoadmapPage() {
  return (
    <StudentPlaceholder
      title="Roadmap"
      message="Your roadmap and tasks are being built in this stage."
    />
  );
}
