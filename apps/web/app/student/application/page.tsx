import { StudentPlaceholder } from "@/components/student/placeholder";
export default function StudentApplicationPage() {
  return (
    <StudentPlaceholder
      title="My Application"
      message="Your applications are built in a later stage."
    />
  );
}
