import { Users } from "lucide-react";
import { StubPage } from "@/components/counsellor/stub-page";

// Placeholder — the real Students grid is built in the next prompt (Stage 2.3).
export default function CounsellorStudentsPage() {
  return (
    <StubPage
      icon={Users}
      title="Students"
      message="The students grid is being built in this stage."
    />
  );
}
