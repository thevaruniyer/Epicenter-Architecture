import { BarChart3 } from "lucide-react";
import { StubPage } from "@/components/counsellor/stub-page";

// Placeholder — reporting is a later addition to the build.
export default function CounsellorReportsPage() {
  return (
    <StubPage
      icon={BarChart3}
      title="Reports"
      message="Caseload and progress reporting is a later addition to the build."
    />
  );
}
