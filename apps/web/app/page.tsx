// Bootstrap placeholder route. Real role-gated surfaces (counsellor + student)
// are built from Stage 2 onward. Structure only — no business logic here yet.
export default function Home() {
  return (
    <main>
      <h1>Epicenter Education</h1>
      <p>Monorepo scaffold. Build in progress.</p>
    </main>
  );
}
