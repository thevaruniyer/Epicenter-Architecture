// @epicenter/ui — shared component library.
// Components (Card, Panel/Dialog, status Pill, AI badge, ...) are built on
// Tailwind + shadcn/ui, themed from the UI/UX Doctrine tokens, in Stage 1
// (Build Runbook Prompt 1.6). Intentionally empty at bootstrap.
export {};
