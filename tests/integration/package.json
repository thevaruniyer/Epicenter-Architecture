{
  "name": "@epicenter/integration-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Integration tests against the Supabase project — RLS enforcement, verified end to end via real authenticated clients.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@epicenter/config": "workspace:*",
    "@supabase/supabase-js": "^2.110.2",
    "@types/node": "^22.10.7",
    "dotenv": "^16.4.7",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
